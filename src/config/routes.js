import React from 'react'
import { Scene, Router } from 'react-native-router-flux'

const RouterComp = () => {
    return (
        <Router>
            <Scene />
        </Router>
    )
}

export default RouterComp
