import {
    Opening,
    Login,
    Register,
    ResetPassword
} from '../containers/authScreens'

export const authScreens = {
    opening: Opening,
    login: Login,
    register: Register,
    resetPassword: ResetPassword
}
